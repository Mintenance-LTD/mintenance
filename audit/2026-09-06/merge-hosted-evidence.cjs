const fs=require('fs');
const p='audit/2026-09-06/READINESS-AUDIT.md';
let s=fs.readFileSync(p,'utf8');
const link='[hosted Supabase verification](<C:/Users/Djodjo.Nkouka.ERICCOLE/Downloads/mintenance-clean/audit/2026-09-06/HOSTED-SUPABASE-VERIFICATION.md>)';
s=s.replace('## A. Coverage and limitations',`**Hosted verification update:** Supabase MCP has now checked both production and staging. Production blocks the demonstrated profile-role UPDATE and older sensitive RPC execution; staging/fresh local do not. Production still permits the unsafe escrow/contract INSERT fields and exposes two newer contribution-reward functions to anonymous callers. See the ${link} for the authoritative environment comparison, exact evidence and repair gates. No hosted data was modified.\n\n## A. Coverage and limitations`);
s=s.replace('| Check | Observed result | Limits |','| Check | Observed result | Limits |');
s=s.replace('| Web and mobile TypeScript |','| Hosted Supabase MCP | Production and staging privileges, policies, defaults, triggers, functions, migrations, views, bucket settings and advisors inspected | Read-only metadata; no customer rows or hosted mutation tests |\n| Hosted HTTP | Auth/settings and zero-row REST queries returned 200; retired payout function returned 410 | No hosted login, customer content or Stripe operation |\n| Web and mobile TypeScript |');
s=s.replace('Deployed infrastructure remains outside the tested scope.','Hosted Supabase infrastructure was subsequently inspected read-only through MCP; the deployed web bundle and hosted money-moving journeys remain outside the verified scope.');
s=s.replace('### F1 — Critical, high confidence: effective grants permit self-promotion to administrator','### F1 — Critical in staging/fresh local; production scope narrowed: effective-grant drift');
const f1='**Hosted scope correction:** Production denies authenticated UPDATE of `profiles.role` and anon/authenticated execution of the inspected deletion/bid/idempotency RPCs. Do not report that specific self-admin exploit as reproduced in production. Staging retains the permissive grants. Production still has unsafe default ACLs and anonymously executable new contribution-reward functions with no caller authorization. These are additional manifestations of the same grant defect; the hosted verification details the evidence, impact and targeted fix.\n\n';
const anchor='### F1 — Critical in staging/fresh local; production scope narrowed: effective-grant drift\n\n';
s=s.replace(anchor,anchor+f1);
s=s.replace('### F2 — Critical, high confidence: clients can fabricate funded escrow and accepted contract/bid state\n\n','### F2 — Critical, high confidence: clients can fabricate funded escrow and accepted contract/bid state\n\n**Hosted evidence:** Production column privileges, participant INSERT policies, CHECK constraints and trigger metadata confirm the unsafe INSERT authority. Actual fabrication was exercised locally only. Production UPDATE restrictions do not repair INSERT authority.\n\n');
s=s.replace('### F7 — High, high confidence: requesting changes fails against the job status trigger\n\n','### F7 — High, high confidence: requesting changes fails against the job status trigger\n\n**Hosted evidence:** MCP retrieved the attached production trigger and its unconditional rejection of transitions out of completed, matching the local failure.\n\n');
s=s.replace('Deployed infrastructure remains outside the tested scope.','Hosted database metadata is now inspected; deployed application behavior remains separate.');
s+='\n## Final repair gate\n\nUse the '+link+' together with this report. It corrects environment scope without discarding valid local reproductions. The primary dependency is a canonical effective-permission contract verified on fresh, upgraded, staging and production schemas; migration history alone failed to preserve that contract. Then repair shared-client identity/object signing and financial recovery, and convert each diagnostic into an expected-safe regression test. The supplied read-only authority snapshot was executed successfully against production and still reports the identified unsafe client authority. No application fixes or hosted mutations were performed.\n';
fs.writeFileSync(p,s);
console.log('Main audit reconciled with hosted evidence.');
