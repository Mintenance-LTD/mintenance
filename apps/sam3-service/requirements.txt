# Core dependencies first (from PyPI)
pip>=25.0
setuptools>=61
wheel

# Image Processing
Pillow==11.0.0
numpy==1.26.4
opencv-python-headless

# Hugging Face (for model downloads)
huggingface-hub==0.24.0

# Web Framework
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# CORS & HTTP
python-multipart==0.0.12
httpx==0.27.2

# Optional: For better performance
aiofiles==24.1.0

# SAM 3 dependencies (discovered during installation)
einops
decord
pycocotools
psutil

# PyTorch with CUDA support (install separately with --index-url)
# torch==2.7.0
# torchvision
# torchaudio
# Install with: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126

# SAM 3 Core (install after PyTorch)
# Install with: pip install -e git+https://github.com/facebookresearch/sam3.git#egg=sam3

