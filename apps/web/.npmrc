# Required for React 19 compatibility with packages that haven't updated peerDependencies
legacy-peer-deps=true

# Inherit supply chain security settings from root .npmrc
# (ignore-scripts, @mintenance scope lock are set at root level)
