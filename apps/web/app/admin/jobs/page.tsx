import { JobManagementClient } from './components/JobManagementClient';

export const metadata = {
  title: 'Job Management | Admin | Mintenance',
};

export default function AdminJobsPage() {
  return <JobManagementClient />;
}
