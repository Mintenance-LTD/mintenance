import { NetworkDiagnosticsService } from '../networkDiagnostics';

describe('NetworkDiagnosticsService', () => {
  it('exports the module', () => {
    expect(NetworkDiagnosticsService).toBeDefined();
  });
});
