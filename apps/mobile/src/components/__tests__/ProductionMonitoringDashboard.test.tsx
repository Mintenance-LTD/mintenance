import ProductionMonitoringDashboard from '../ProductionMonitoringDashboard';

describe('ProductionMonitoringDashboard', () => {
  it('exports a component', () => {
    expect(ProductionMonitoringDashboard).toBeDefined();
    expect(typeof ProductionMonitoringDashboard).toBe('function');
  });
});
