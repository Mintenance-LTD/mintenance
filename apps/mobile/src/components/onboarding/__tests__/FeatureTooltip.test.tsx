import FeatureTooltip from '../FeatureTooltip';

describe('FeatureTooltip', () => {
  it('exports a component', () => {
    expect(FeatureTooltip).toBeDefined();
    expect(typeof FeatureTooltip).toBe('function');
  });
});
