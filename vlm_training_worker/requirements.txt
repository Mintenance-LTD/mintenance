# Mint AI VLM Training Worker — Python dependencies
# These are the packages baked into the Modal image at build time.
# Do NOT install these locally for production — Modal handles the image.
# For local development/testing of the worker scripts only:
#   pip install -r vlm_training_worker/requirements.txt

# ---- Core ML ----
torch==2.4.1
torchvision==0.19.1
transformers>=4.49.2    # 4.49.2+ for Qwen2_5_VLForConditionalGeneration
peft==0.13.2            # LoRA / QLoRA via PEFT
trl==0.12.1             # SFTTrainer (used by LLaMA-Factory internally)
accelerate>=1.4.0       # multi-GPU / device_map="auto" support
bitsandbytes>=0.44.1    # QLoRA 4-bit NF4 quantisation (optional)

# ---- LLaMA-Factory ----
llamafactory>=0.9.1     # Unified fine-tuning framework (replaces custom trainer)

# ---- Model-specific ----
qwen-vl-utils==0.0.8   # Qwen2.5-VL processor helpers

# ---- Data ----
datasets==3.2.0         # HuggingFace datasets for train/test split

# ---- Storage / HTTP ----
supabase==2.10.0        # Supabase Python client
httpx==0.27.2           # Async HTTP — signed URL generation, callbacks

# ---- Image ----
pillow==11.0.0

# ---- Inference endpoint only ----
fastapi==0.115.5
uvicorn==0.32.1

# ---- Modal SDK (for local modal deploy/serve commands) ----
modal>=0.64.0
