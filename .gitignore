# Node / Yarn / NPM
node_modules/
.npm/
.yarn/
.pnpm-store/

# Build artifacts
.next/
.out/
dist/
build/
coverage/
*.tsbuildinfo
*.log

# Expo / React Native
.expo/
.expo-shared/
android/
ios/
*.keystore
*.jks

# EAS
*.ipa
*.apk
*.aab

# Env & credentials
.env*
!.env.example
credentials/
*.pem
*.p12
*.mobileprovision
google-services.json
GoogleService-Info.plist
infrastructure/**/*.env
infrastructure/**/*.pem
infrastructure/**/*.key

# Editor/OS files
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Cache
.cache/
.tmp/
.expo/web/cache/

# Generated
apps/web/tsconfig.tsbuildinfo
apps/mobile/tsconfig.tsbuildinfo
packages/*/tsconfig.tsbuildinfo

# Misc
mintenance-complete.bundle

# Learn more https://docs.github.com/en/get-started/getting-started-with-git/ignoring-files

# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
.kotlin/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store
*.pem

# Environment and Configuration Files
.env
.env.local
.env.development
.env.staging
.env.production
.env*.local
.env.backup*
.cursor/mcp.local.json
.cursor/mcp.json
.cursor/mcp.json.*
!.cursor/mcp.json.example
.claude/settings.local.json
# Audit re-review (2026-04-29): Claude Code agent worktrees live
# inside the repo at `.claude/worktrees/<branch>` and each has a
# `.git` file pointing back at the main repo's
# `.git/worktrees/<branch>/` registration. Without this ignore,
# `git status` sees every worktree as a "modified submodule" and
# floods the output with `m .claude/worktrees/...` lines. Some
# Windows shells then mangle the gitdir path ("not a git
# repository: .claude/worktrees/X/C:/Users/.../.git/worktrees/X")
# when bash mixes forward slashes with the `C:` drive prefix —
# fixing the noise at the source closes that whole class of
# transient `git status` failures.
.claude/worktrees/

# Test output artifacts
**/test-results.json
**/*-test-results*.txt
**/test-results/

# TypeScript
*.tsbuildinfo

# Security & Credentials - CRITICAL: Never commit these
credentials/*.p8
credentials/*.json
credentials/*.env
!credentials/README.md
!credentials/.gitkeep
service-account*.json
AuthKey_*.p8
*.key
*.pem
*.p12
*.keystore
google-services.json
GoogleService-Info.plist

# API Keys and Tokens
**/config/**/secrets.json
**/config/**/private.json
**/*-key.json
**/*-secret.json

# Stripe Keys
stripe-keys.json
stripe-secret.json
STRIPE_ENV_SETUP.txt
**/STRIPE_ENV_SETUP.txt

# Database
*.db
*.sqlite
*.sqlite3

# Logs with potential sensitive data
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Temporary files
tmp/
temp/
.tmp/
.temp/

# ⚠️ SECURITY CRITICAL: Server-side environment files
.env.server
.env.server.local
.env.server.*

# Security audit reports
security-audit-report.txt
audit-log.txt
vulnerability-report.*

# Additional sensitive directories
secrets/
private-keys/
server-credentials/

.vercel

# Windows NUL files (device files)
**/NUL
node_modules

# Training data and datasets (large files, long paths)
Building Defect Detection 7.v2i.yolov11/
yolo_dataset/
yolo_dataset_*/
training_data/
training-data/
all_images_merged/
colab_upload/
colab_upload_v3/
*.pt
*.onnx
*.zip
best_model_*.pt
best_model_*.onnx
yolo11n.pt

# Exclude temporary/generated markdown files (but NOT documentation)
# NOTE: Removed blanket *.md rule - docs should be version controlled
/tmp/**/*.md
/runs/**/*.md

# Development artifacts that shouldn't be in root
*.sql
!supabase/migrations/**/*.sql
!migrations/**/*.sql
*.py
!scripts/**/*.py
!apps/**/*.py
*.ps1
!scripts/**/*.ps1
*.txt
!README.txt
*.tmp
*.msi
*.bat
!scripts/**/*.bat
*.sh
!scripts/**/*.sh
!infrastructure/**/*.sh
*.json
!package*.json
!tsconfig*.json
!*.config.json
!class_mapping*.json
*.html
!index.html
*.js
!scripts/**/*.js
!*.config.js
!jest*.js
!playwright*.js
!lighthouserc.js
!mcp-server.js
!mcp-server.ts
# Expo config plugins (hand-written source, not generated output)
!apps/mobile/plugins/**/*.js

# Test files and debug artifacts in root
test-*.js
test-*.ts
*-test.js
*-test.ts
debug-*.png
debug-*.jpg
*-result.png
*-result.jpg
*-test-result.png
*-test-result.jpg

# Jupyter notebooks (should be in notebooks/ or docs/)
*.ipynb
!docs/**/*.ipynb

# Config files that should be in specific directories
security-*.config.js
security-*.cron
performance-*.config.js
auth-*.html

# Root-level test/debug/training artifacts (do not commit)
/playwright-report/
/Screenshots/
/batch_test_results/
/beta-testing-data/
/merged_dataset_test/
/test_results/
/test-results/
/temp-supabase-mock/
/runs/
/components/
/jest-setup.backup.js
/metro.config.backup.js
/app.json
/index.html
/create_class_mapping.py
/class_mapping.json
/class_mapping_dataset6.json
# Malformed Windows path artifacts
assets/c__*
c:Users*/

# Python virtual environments
**/venv/
**/.venv/
__pycache__/

# SAM service artifacts
apps/sam3-service/model_cache/
apps/sam3-service/sam3_repo/
# Local training data export artifacts (run scripts/vlm-training/export_bootstrap_dataset.mjs to regenerate)
training_data*.jsonl
